/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet';
import * as web3 from '@solana/web3.js';
import { Key, keyBeet } from '../types/Key';

/**
 * Arguments used to create {@link StakeManager}
 * @category Accounts
 * @category generated
 */
export type StakeManagerArgs = {
  key: Key;
  minStakeTime: beet.bignum;
};
/**
 * Holds the data for the {@link StakeManager} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class StakeManager implements StakeManagerArgs {
  private constructor(readonly key: Key, readonly minStakeTime: beet.bignum) {}

  /**
   * Creates a {@link StakeManager} instance from the provided args.
   */
  static fromArgs(args: StakeManagerArgs) {
    return new StakeManager(args.key, args.minStakeTime);
  }

  /**
   * Deserializes the {@link StakeManager} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0,
  ): [StakeManager, number] {
    return StakeManager.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link StakeManager} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
  ): Promise<StakeManager> {
    const accountInfo = await connection.getAccountInfo(address);
    if (accountInfo == null) {
      throw new Error(`Unable to find StakeManager account at ${address}`);
    }
    return StakeManager.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Deserializes the {@link StakeManager} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [StakeManager, number] {
    return stakeManagerBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link StakeManager} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return stakeManagerBeet.serialize(this);
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link StakeManager}
   */
  static get byteSize() {
    return stakeManagerBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link StakeManager} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(StakeManager.byteSize, commitment);
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link StakeManager} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === StakeManager.byteSize;
  }

  /**
   * Returns a readable version of {@link StakeManager} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      key: 'Key.' + Key[this.key],
      minStakeTime: this.minStakeTime,
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const stakeManagerBeet = new beet.BeetStruct<StakeManager, StakeManagerArgs>(
  [
    ['key', keyBeet],
    ['minStakeTime', beet.i64],
  ],
  StakeManager.fromArgs,
  'StakeManager',
);
